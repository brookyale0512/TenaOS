# Dependencies and generated frontend output
node_modules/
TenaOS-Frontend/node_modules/
dist/
TenaOS-Frontend/dist/
dist-ssr/
*.tsbuildinfo

# Local environment and secrets
.env
.env.*
!.env.example
*.local
*.secret
*.secrets
*-key.json
*service-account*.json
vertex-key.json

# Runtime and database artifacts
*.sql
*.sql.gz
*.dump
*.bak
*.sqlite
*.sqlite3
runtime-artifacts/
TenaAgent/runtime/
backups/

# Model weights and large binary artifacts — fetched from HuggingFace, never committed.
# See scripts/fetch-models.sh.
models/*.gguf
models/*.litertlm
models/*.safetensors
*.gguf
*.litertlm
*.safetensors
*.npy

# Build artifacts
*.zip

# Personal documents
resume_build/
*.resume.pdf

# Logs and temporary files
*.log
logs/
tmp/
temp/
.cache/
.pytest_cache/
__pycache__/
*.py[cod]

# Editor and OS files
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Vendored upstream binaries — not committed; reconstructed at build time
# (Dockerfile clones the pinned llama.cpp tag) or fetched via scripts/fetch-models.sh.
third_party/
